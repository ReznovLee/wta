# Environment configuration (deterministic keys)
horizon: 200
time_step: 1.0
max_interceptors: 3
max_targets: 3
weapon_specs:
  max_range_km: 100
  reload_time_s: 20
  ammo_per_unit: 4
hard_constraints:
  one_to_one: true
  ammo_budget: true
  range_limit: true
  time_window: true
observation_features:
  include_velocity: true
  include_value: true
  include_threat: true