# Data configuration
offline_dataset:
  root: "data/trajectories"
  format: "pt"
  num_episodes: 500
  max_len: 256
scenario:
  seeds: [0, 1, 2, 3, 4]
  time_to_impact: 1000
  dispersion_rate: 0.6
split:
  train: 0.8
  val: 0.2