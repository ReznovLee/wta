# Data configuration
offline_dataset:
  root: "data/trajectories"
  format: "npz"
  num_episodes: 20
  max_len: 128
scenario:
  seeds: [0, 1]
  time_to_impact: 300
  dispersion_rate: 0.6
split:
  train: 0.8
  val: 0.2
